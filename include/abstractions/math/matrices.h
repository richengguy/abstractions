#pragma once

#include <abstractions/math/types.h>

#include <random>
#include <type_traits>

namespace abstractions
{

// TODO: Need to figure this out

// template<typename G, typename TDist>
// Matrix RandomMatrix(int rows, int cols, TDist &distribution, )
// {
//     static_assert(std::is_same<Matrix::Scalar, typename TDist::result_type>::value, "Matrix type and distribution type must match.");

// }

}
